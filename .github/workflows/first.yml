name: Shell command

on: [push]

jobs:
  run-shell-command:
    run-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello wolrd"
      - name: multiline script
        run: |
          node -v
          npm -v
