name: Run jobs by order

on: [push]

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "first-job"

  third-job:
    needs: ["second-job"]
    runs-on: ubuntu-latest
    steps:
      - name: echo second-jobs
        run: echo "third-job"

  second-job:
    runs-on: ubuntu-latest
    needs: ["first-job"]
    steps:
      - name: echo second-job
        run: echo "second-job"
